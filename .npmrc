# React 19 RC is still peer-dep-shy: several UI libs pin ^18. Locally we
# always install with --legacy-peer-deps; on Vercel/CI the same behavior
# needs to be a repo-level default, otherwise `npm ci` fails with ERESOLVE.
legacy-peer-deps=true
